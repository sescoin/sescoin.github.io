<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- Nom de l'app affiché sur le téléphone -->
    <key>CFBundleName</key>
    <string>SES Coin</string>

    <key>CFBundleDisplayName</key>
    <string>SES Coin</string>

    <!-- Infos standard Flutter -->
    <key>CFBundleDevelopmentRegion</key>
    <string>fr</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>

    <key>CFBundleSignature</key>
    <string>????</string>

    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <!-- Langue par défaut : français -->
    <key>CFBundleLocalizations</key>
    <array>
        <string>fr</string>
        <string>en</string>
    </array>

    <!-- Caméra (scan QR Code) -->
    <key>NSCameraUsageDescription</key>
    <string>SES Coin utilise la caméra pour scanner les QR Codes de paiement</string>

    <!-- Galerie photo (photo de profil) -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>SES Coin accède à ta galerie pour choisir une photo de profil</string>

    <!-- NFC -->
    <key>NFCReaderUsageDescription</key>
    <string>SES Coin utilise le NFC pour les paiements en face à face</string>

    <!-- AID ISO 7816 autorisés (pour lire le HCE d'un Android) -->
    <!-- Entitlement miroir dans Runner.entitlements — à lier dans Xcode -->
    <key>com.apple.developer.nfc.readersession.iso7816.select-identifiers</key>
    <array>
        <string>F0534553434F494E</string>
    </array>

    <!-- Notifications push -->
    <key>UIBackgroundModes</key>
    <array>
        <string>fetch</string>
        <string>remote-notification</string>
    </array>

    <!-- Orientation écran -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Flutter -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

</dict>
</plist>
